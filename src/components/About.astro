---
const stats = [
  { value: "12+", label: "Years on the road" },
  { value: "18k", label: "Happy travelers" },
  { value: "40+", label: "Destinations" },
  { value: "4.9", label: "Average rating" },
];
---

<section id="about" class="bg-stone-950 py-24 text-white">
  <div
    class="mx-auto grid max-w-7xl items-center gap-16 px-6 lg:grid-cols-2"
  >
    <div class="reveal overflow-hidden rounded-3xl">
      <img
        src="https://images.unsplash.com/photo-1469854523086-cc02fe5d8800?auto=format&fit=crop&w=1200&q=75"
        alt="Camper van driving along a winding coastal road"
        loading="lazy"
        class="aspect-[4/5] h-full w-full object-cover sm:aspect-[4/3] lg:aspect-[4/5]"
      />
    </div>
    <div class="reveal">
      <p
        class="text-sm font-semibold uppercase tracking-[0.25em] text-brand-400"
      >
        About us
      </p>
      <h2 class="mt-3 font-display text-4xl font-semibold sm:text-5xl">
        Born from a road trip that never really ended
      </h2>
      <p class="mt-6 text-lg leading-relaxed text-white/75">
        Terraventura started with two friends, one battered van and a map full
        of pencil marks. Today we design trips and keep a fleet ready for
        travelers who want the same thing we did: freedom, real places and
        zero hassle.
      </p>
      <p class="mt-4 text-lg leading-relaxed text-white/75">
        Every itinerary is scouted in person, every car serviced by our own
        team, and every guest treated like a friend joining the trip.
      </p>
      <dl class="mt-12 grid grid-cols-2 gap-8 sm:grid-cols-4">
        {
          stats.map((stat) => (
            <div>
              <dt class="font-display text-4xl font-semibold text-brand-400">
                {stat.value}
              </dt>
              <dd class="mt-1 text-sm text-white/60">{stat.label}</dd>
            </div>
          ))
        }
      </dl>
    </div>
  </div>
</section>
